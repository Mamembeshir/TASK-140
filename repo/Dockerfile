FROM alpine:3.20
LABEL maintainer="ForgeFlow"
LABEL description="ForgeFlow is a native iOS app — no server component. This container exists for CI compatibility."
RUN echo "ForgeFlow: native iOS app, no server required."
CMD ["echo", "ForgeFlow build placeholder — success"]
